# Using hiera data instead of params.pp
---
version: 5

hierarchy:
  - name: "Per-OS defaults"
    path: "os/%{facts.os.family}.yaml"

  - name: Common
    path: common.yaml

defaults:
  data_hash: yaml_data
  datadir: data